---
import Welcome from "../components/Welcome.astro"
import Layout from "../layouts/Layout.astro"
import { loadQuery } from "../sanity/services/load-query"

const { data: posts } = await loadQuery({ query: `*[_type == "author"][0]` })
---

<Layout>
	<Welcome />
</Layout>
